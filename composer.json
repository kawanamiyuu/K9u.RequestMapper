{
    "name": "k9u/router",
    "type": "library",
    "description": "Annotatable Router library for PHP",
    "keywords": ["router", "routing", "annotation"],
    "license": "MIT",
    "authors": [
        {
            "name": "Yuu Kawanami",
            "email": "yuu.kawanami@gmail.com"
        }
    ],
    "minimum-stability": "stable",
    "require": {
        "php": ">=7.2"
    },
    "require-dev": {
        "phpmd/phpmd": "^2.7",
        "phpstan/phpstan": "^0.11",
        "phpunit/phpunit": "^8.4",
        "squizlabs/php_codesniffer": "^3.5"
    },
    "autoload": {
        "psr-4": {
            "K9u\\Router\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "K9u\\Router\\": "tests/"
        }
    },
    "scripts": {
        "check":["@lint", "@test"],
        "test": "phpdbg -qrr ./vendor/bin/phpunit --coverage-text",
        "lint": [
            "phpcs --standard=PSR12 src",
            "phpmd src text phpmd.xml",
            "phpstan analyze --no-progress --level=max src"
        ],
        "fmt": "phpcbf --standard=PSR12 src"
    },
    "config": {
        "sort-packages": true
    }
}
